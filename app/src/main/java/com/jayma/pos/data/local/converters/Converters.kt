package com.jayma.pos.data.local.converters

import androidx.room.TypeConverter

class Converters {
    // Add type converters here if needed for complex types
    // Currently using separate tables for relationships, so no converters needed
}
